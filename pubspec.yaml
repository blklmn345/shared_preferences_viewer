name: shared_preferences_viewer
description: A new Flutter package project.
version: 0.0.1
repository: https://github.com/blklmn345/shared_preferences_viewer

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
